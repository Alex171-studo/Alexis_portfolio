#include <stdio.h>
#include <math.h>

void is_premier(int a){
    int b = sqrt(a);
    int compteur = 0;
    for (int i = 1 ; i<= b; i++){
        if ( ( a/i) == 0)
            compteur++;
    }
    if
}

int main(void){



    return 0;
}